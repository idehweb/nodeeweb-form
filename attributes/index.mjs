
import model from './model.mjs'
import routes from './routes.mjs'

export default {
    "name": "attributes",
    "model": model,
    "modelName": "Attributes",
    "routes": routes,

    "views": [{
        "func": (req, res, next) => {
        }
    }],
    "edits": [{
        "func": (req, res, next) => {
        }
    }],

}